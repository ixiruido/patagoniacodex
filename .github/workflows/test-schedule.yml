name: Test Schedule

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: echo "SCHEDULE FUNCIONA $(date -u)"
